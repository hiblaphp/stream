{
    "name": "hiblaphp/stream",
    "type": "library",
    "description": "A PHP library for working with non-blocking streams of data",
    "license": "MIT",
    "prefer-stable": true,
    "require": {
        "hiblaphp/event-loop": "^1.0@alpha",
        "hiblaphp/promise": "^1.0@alpha",
        "evenement/evenement": "^3.0"
    },
    "autoload": {
        "psr-4": {
            "Hibla\\Stream\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Tests\\": "tests/"
        }
    },
    "require-dev": {
        "laravel/pint": "^1.10",
        "pestphp/pest": "^4.0",
        "phpstan/phpstan": "^2.1",
        "phpstan/phpstan-strict-rules": "^2.0",
        "phpstan/extension-installer": "^1.4"
    },
    "config": {
        "allow-plugins": {
            "pestphp/pest-plugin": true,
            "phpstan/extension-installer": true
        }
    },
    "scripts": {
        "test": "./vendor/bin/pest",
        "format": "./vendor/bin/pint",
        "format-test": "./vendor/bin/pint --test",
        "analyze": "./vendor/bin/phpstan analyse"
    }
}
